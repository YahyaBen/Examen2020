@model Examen_YB.Models.Patient

@{
    ViewData["Title"] = "Declarer_Deces";
}

<h1>Declarer_Deces</h1>

<h4>Patient</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Declarer_Deces">
            @Html.DisplayFor(modelItem => Model.Nom) @Html.DisplayFor(modelItem => Model.Prenom) est Deceede
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
